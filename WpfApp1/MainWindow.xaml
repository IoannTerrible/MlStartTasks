<Window
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:av="http://schemas.microsoft.com/expression/blend/2008" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" mc:Ignorable="av" x:Class="WpfApp1.MainWindow"
        Title="MainWindow" Height="450" Width="930">
    <Window.Resources>
        <SolidColorBrush x:Key="BackgroundColor" Color="#FFFFEDED"/>
        <SolidColorBrush x:Key="ButtonColor" Color="Black"/>
    </Window.Resources>
    <Grid Background="{StaticResource BackgroundColor}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="447*"/>
            <ColumnDefinition Width="14*"/>
        </Grid.ColumnDefinitions>

        <Button x:Name="button" Click="button_Click" Width="100" Height="30" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="0,10,28,0" Grid.ColumnSpan="2" Content="Click Me!"/>
        <Frame x:Name="MainFrame"  Margin="14,10,105,44"/>
        <ListBox x:Name="StoryListBox" HorizontalAlignment="Left" Height="34" Margin="14,390,0,0" VerticalAlignment="Top" Width="783" av:ItemsSource="{av:SampleData ItemCount=5}"/>
    </Grid>
</Window>
